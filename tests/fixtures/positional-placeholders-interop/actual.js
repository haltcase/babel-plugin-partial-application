const isObjectPropertyNaN = _`1`.length !== _`1`.length

const getNameLengthAsString = _`1`.name.length.toString()

const positionalWithSpread = fn(_`1`, ..._)

const multiplePlaceholders = fn(_, _, _`1`, _.name._, ..._)
